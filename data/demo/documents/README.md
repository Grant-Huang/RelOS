# 演示文档样例

本目录用于存放可直接在 `知识训练 -> 企业文档标注` 页面上传的演示文档。

建议优先使用以下文件：

- `cmms_workorder_m3.xlsx`：维修工单，适合演示设备故障知识沉淀
- `supplier_delay_q235.xlsx`：供应商交期表，适合演示供应链与工单阻塞知识沉淀
- `shift_handover_m3.docx`：交接班日志，适合演示班组经验与异常交接知识沉淀

使用前请确认：

1. 后端 API 已启动
2. 当前环境允许文档抽取（开发环境可用 mock）
3. 上传格式使用 `xlsx` 或 `docx`
